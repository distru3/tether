# Icons

Placeholder 1×1 PNG so `tauri build` has something to point at.

Replace with the real icon set before the first tagged release:

    npm run tauri -- icon path/to/source-1024x1024.png

Tauri generates the full platform-specific set (`.ico`, `.icns`, sized PNGs)
into this directory.
